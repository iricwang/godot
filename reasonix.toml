# Reasonix configuration.
# Resolution order: flag > ./reasonix.toml > ~/AppData/Roaming/reasonix/config.toml > built-in defaults.
# Secrets come from the environment via api_key_env; never put keys here.

config_version = 2   # schema marker for diagnostics; old versions may ignore it
default_model = "deepseek-flash"
# language      = "zh"   # ui/model language; empty = auto-detect from $LANG / $REASONIX_LANG

[agent]
# system_prompt = """..."""   # omit to use the built-in prompt for this version
# system_prompt_file = "prompts/system.md"   # overrides system_prompt when set
# max_steps         = 0   # executor tool-call rounds; 0 = no limit
# planner_max_steps = 12   # planner read-only tool-call rounds; 0 = no limit
temperature       = 0.0
auto_plan   = "off"   # off|on; off keeps plan mode manual
# reasoning_language = "zh"   # visible reasoning language: auto|zh|en
# auto_plan_classifier = "deepseek-flash"   # optional; only used for borderline tasks
soft_compact_ratio  = 0.5   # notice only; keeps cache-first prefix intact
compact_ratio       = 0.8   # try compacting when prompt reaches this fraction
compact_force_ratio = 0.9   # force compacting at this high-water mark
# keep                = ["errors"]   # compaction keep policy: errors, user_marked
# recent_keep         = 2   # minimum recent messages kept verbatim
cold_resume_prune   = true   # elide stale tool results when reopening a session past the provider cache window
# planner_model = "mimo"   # optional: enable two-model collaboration
# subagent_model = "deepseek-pro"   # optional default for runAs=subagent skills
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }   # per-skill overrides
# subagent_effort = "high"   # optional default effort for subagents
# subagent_efforts = { review = "max", task = "high" }   # per-tool/skill effort overrides
# output_style = "explanatory"   # explanatory | learning | concise | custom; empty = default

[tools]
enabled = []   # empty = all built-in tools
bash_timeout_seconds = 120   # foreground safety cap; set 0 for no tool-local cap

[tools.background_jobs]
stalled_warning_seconds = 900   # warn once per background job after this many quiet seconds; 0 disables

[lsp]
enabled = true   # language server tools; servers launch lazily when used
# [lsp.servers.go]
# command = "gopls"
# args = []
# extensions = [".go"]

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # extra custom skill roots
# excluded_paths = ["~/.agents/skills"]   # hide convention roots without deleting folders
# max_depth = 3   # nested scan depth; set 1 for legacy root-only discovery
# disabled_skills = ["review"]   # hide noisy or unwanted skills

[permissions]
# Per-call gating. mode = writer fallback when no rule matches: ask|allow|deny.
# Readers always default to allow. Precedence: deny > ask > allow > fallback.
# Rules are "Tool" or "Tool(specifier)"; e.g. Bash(go test:*), Edit(src/**).
mode  = "ask"
# deny = ["Bash(rm -rf*)", "Bash(git push*)"]   # hard-blocked in every mode
allow = ["Bash(cd /d D:\\AI_Temp\\Godot\\godot-4.7-beta && dir build.bat 2>&1 | head -3)", "Bash(cd \"D:/AI_Temp/Godot/godot-4.7-beta\" && python -m SCons platform=windows target=editor tests=no game_framework=yes psd_ui=yes -j8 progress=no 2>&1 | tail -80)", "Bash(cd \"D:/AI_Temp/Godot/godot-4.7-beta\" && powershell -Command \"Get-Process | Where-Object {\\$_.Path -like '*godot.windows.editor*'} | Select-Object Id,ProcessName,Path\" 2>&1)", "Bash(cd \"D:/AI_Temp/Godot/godot-4.7-beta\" && python -m SCons platform=windows target=editor tests=no game_framework=yes psd_ui=yes -j8 progress=no 2>&1 | grep -E \"Compiling editor.docks|main_screen_dock|error|Error|FAILED|^scons:\" | head -30)", "explore", "Bash(cd /d/AI_Temp/Godot/godot-4.7-beta && git log --oneline -20 2>&1 | head -30)", "run_skill", "Bash(python --version && python -m SCons --version)", "Bash(python -m:*)", "Bash(git status --short && git diff --stat)", "review", "Bash(cd /d/Proj-Godot/godot && git log --oneline -30)", "Bash(cd /d/Proj-Godot/godot && git status)", "Bash(cd /d/Proj-Godot/godot && git log --oneline --all -- editor/run/game_view_plugin.cpp editor/run/embedded_process.cpp editor/gui/window_wrapper.cpp 2>&1 | head -40)", "Bash(cd /d/Proj-Godot/godot && git log --oneline --all --grep=\"floating\" -- editor/ 2>&1 | head -20)", "Bash(cd /d/Proj-Godot/godot && git log --oneline --all --grep=\"close\" -- editor/run/ editor/gui/window_wrapper* 2>&1 | head -20)", "Bash(cd /d/Proj-Godot/godot && git log --oneline --all --grep=\"crash\" -- editor/run/ 2>&1 | head -20)", "Bash(cd /d/Proj-Godot/godot && git show --stat 0baa47bb53)", "Edit", "Bash(cd /d/Proj-Godot/godot && grep -n \"^#include\" platform/windows/display_server_windows.cpp | head -10)", "Bash(cd /d/Proj-Godot/godot && ls bin/godot.windows.editor.x86_64.exe 2>&1 | head -3)", "Bash(cd /d/Proj-Godot/godot && cat build.bat)", "Bash(cd /d/Proj-Godot/godot && python -m SCons platform=windows target=editor tests=yes game_framework=yes psd_ui=yes -j31 --debug=time 2>&1 | tail -80)", "mcp__godot-ai__session_manage", "mcp__godot-ai__scene_get_hierarchy", "mcp__godot-ai__node_get_properties", "mcp__godot-ai__project_manage", "mcp__godot-ai__filesystem_manage", "Bash(cd /d/Proj-Godot/Simple && grep -n \"1080\\|1920\\|1170\\|2532\" modules/plinko_game/scenes/game.gd 2>&1 | head -30)", "mcp__godot-ai__script_patch", "Bash(cd /d/Proj-Godot/Simple && grep -n \"Background\\|set_anchors_preset(Control.PRESET_FULL_RECT)\" modules/plinko_game/scenes/game.gd | head -20)", "mcp__godot-ai__script_manage", "mcp__godot-ai__editor_state", "mcp__godot-ai__logs_read", "mcp__godot-ai__project_run", "Bash(sleep 3:*)", "mcp__godot-ai__editor_screenshot", "mcp__godot-ai__game_manage", "mcp__godot-ai__editor_manage", "Bash(sleep 2:*)", "Bash(cd /d/Proj-Godot/godot && git status && echo \"----\" && git diff --stat)", "Bash(cd /d/Proj-Godot/godot && git log --oneline -20)", "Bash(cd /d/Proj-Godot/godot && git show --stat 657e421a73)", "Bash(cd /d/Proj-Godot/godot && git show 657e421a73)", "Bash(cd /d/Proj-Godot/godot && grep -n \"use_custom_anchors\\|use_anchors\" scene/gui/control.cpp | head -20)", "Bash(cd /d/Proj-Godot/godot && grep -n \"data.anchor\\[SIDE_LEFT\\] != data.anchor\\[SIDE_RIGHT\\]\" scene/gui/control.cpp | head -5)", "Bash(cd /d/Proj-Godot/godot && python -m SCons platform=windows target=editor tests=yes game_framework=yes psd_ui=yes -j31 --debug=time 2>&1 | tail -40)", "Bash(cd /d/Proj-Godot/godot && grep -n \"EditorInspectorPlugin\\|class .*EditorInspectorPlugin\\|add_inspector_plugin\\|_can_handle\\|_parse_property\\|_parse_begin\\|add_property_editor\" editor/scene/gui/control_editor_plugin.cpp | head -40)", "Bash(cd /d/Proj-Godot/godot && grep -n \"ControlOffsetTransformEditor\\|class ControlOffsetTransformEditor\" editor/scene/gui/control_editor_plugin.h editor/scene/gui/control_editor_plugin.cpp 2>&1 | head -20)", "Bash(cd /d/Proj-Godot/godot && grep -n \"is_anchor_offset_property_name\" scene/gui/control.cpp | head -10)", "Bash(cd /d/Proj-Godot/godot && python -m SCons platform=windows target=editor tests=yes game_framework=yes psd_ui=yes -j31 2>&1 | tail -15)", "Bash(cd /d/Proj-Godot/godot && grep -n \"class EditorPropertyFloat\\|EditorPropertyFloat(\" editor/inspector/editor_properties.h | head -10)", "Bash(cd /d/Proj-Godot/godot && grep -n \"set_label\\|void set_label\" editor/inspector/editor_inspector.h | head -10)", "Bash(cd /d/Proj-Godot/godot && grep -n \"EditorPropertyRangeHint\\|range_hint_from_props\" editor/inspector/editor_properties.h editor/inspector/editor_property_helpers.h 2>&1 | head -10)", "Bash(cd /d/Proj-Godot/godot && grep -n \"_parse_range_hint\\|range_hint = _parse\" editor/inspector/editor_properties.cpp 2>&1 | head -10)", "Bash(cd /d/Proj-Godot/godot && grep -n \"class EditorInspectorPluginControl\" editor/scene/gui/control_editor_plugin.h)", "Bash(cd /d/Proj-Godot/godot && grep -n \"#include\" editor/scene/gui/control_editor_plugin.cpp | head -20)", "Bash(cd /d/Proj-Godot/godot && grep -n \"void.*add_property_editor\\|add_property_editor(\" editor/inspector/editor_inspector.h | head -10)", "Bash(cd /d/Proj-Godot/godot && git status -s scene/gui/control.cpp editor/scene/gui/control_editor_plugin.cpp)", "Bash(cd /d/Proj-Godot/godot && git diff scene/gui/control.cpp editor/scene/gui/control_editor_plugin.cpp | head -150)", "Bash(cd /d/Proj-Godot/godot && git add scene/gui/control.cpp editor/scene/gui/control_editor_plugin.cpp && git commit -m \"Inspector: anchors preset + Transform 联动 (Unity RectTool 风格)\n\n- Control::_validate_property 改用 per-axis stretch 判据 (data.anchor[SIDE_*]),\n  覆盖 PRESET_FULL_RECT / 6 个 *_WIDE / 任意 stretch 自定义 anchor:\n  * size 与 position 在任一轴 stretch 时设为 READ_ONLY\n    (与引擎 _set_size 的 size-overridden warning 同源判据)\n  * 该 stretch 轴上的 offset_left/right/top/bottom 在非 custom-anchor 时\n    也强制露出, 让用户能直接编辑矩形的边\n- EditorInspectorPluginControl::parse_property 拦截 offset_left/top/right/bottom:\n  在对应轴 stretch 时, 用 EditorPropertyFloat (suffix=\\\"px\\\") 替代默认 widget,\n  并通过 add_property_editor(p_label) 把标签改成 Left/Top/Right/Bottom,\n  匹配 Unity RectTransform 的字段命名\n\n效果对应:\n  TOP_LEFT 等单点 preset    \\xe2\\x86\\x92 Position + Size 可编辑, 无 offset 字段\n  *_WIDE / VCENTER_WIDE   \\xe2\\x86\\x92 Position/Size 灰, stretch 轴露出 Left/Right 或 Top/Bottom\n  FULL_RECT                \\xe2\\x86\\x92 Position/Size 灰, 4 个 Left/Top/Right/Bottom 全显\n  自定义 stretch anchor    \\xe2\\x86\\x92 Position/Size 灰, 全部 anchor_/offset_/grow_ 字段保留\n\n之前的 commit 657e421a73 只针对 PRESET_FULL_RECT, 此处把它一般化到所有 stretch 场景, 并补上标签改造。\" && echo \"----\" && git log --oneline -3)", "Bash(curl -s ifconfig.me 2>/dev/null || curl -s ip.sb 2>/dev/null || curl -s icanhazip.com 2>/dev/null)", "Bash(hostname -I 2>/dev/null || ip addr show 2>/dev/null | grep -E 'inet ' | grep -v '127.0.0.1' | awk '{print $2}')", "Bash(pwd && git status && git remote -v && git branch --show-current)"]
# ask = ["Edit(src/**)"]   # force a prompt even if otherwise allowed

[sandbox]
# Confine tool blast radius. File-writers (write_file/edit_file/multi_edit/move_file)
# may only write under workspace_root (empty = current dir) and allow_write extras.
# bash = "enforce" (default) jails each command in an OS sandbox (macOS now;
# graceful fallback elsewhere); "off" disables it. network allows egress.
# workspace_root = ""            # default: current working directory
# allow_write = ["/tmp"]          # extra dirs writers may also modify
bash    = "enforce"
network = true

[statusline]
# A custom status line: a command whose first stdout line replaces the built-in
# data row. It receives {"model","contextUsed","contextWindow","cwd"} as JSON on stdin.
# command = "my-statusline.sh"

# External MCP servers. type: "stdio" (default, a subprocess) | "http" | "sse".
# ${VAR} / ${VAR:-default} are expanded from the environment in command/args/env/url/headers.
# [[plugins]]
# name    = "example"
# command = "reasonix-plugin-example"
# [[plugins]]                                  # a remote server over Streamable HTTP
# name    = "stripe"
# type    = "http"
# url     = "https://mcp.stripe.com"
# headers = { Authorization = "Bearer ${STRIPE_KEY}" }
